export const i18n = Object.freeze({
  TAB_ITEMS: [
    'All',
    'Favorites',
    // 'Category'
  ]
});

export const TAB_ATTR = Object.freeze({
  ID: 'filter-tab',
});

export const TAB_PANEL_ATTR = Object.freeze({
  ID: `${TAB_ATTR.ID}-panel`,
});
